aux_source_directory(. SOURCES)
aux_source_directory(xorg SOURCES)
add_library(X11 SHARED ${SOURCES})
target_link_libraries(X11 be translation iconv)
set_target_properties(X11 PROPERTIES SOVERSION 6)
